import axios from 'axios'
const baseUrl = '/api/player'

const searchPlayer = async (input) => {
  // TODO: Etsi kannasta pelaajaa syötteen mukaan

  return input
}

export default {
  searchPlayer
}